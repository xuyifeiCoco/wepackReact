root = true  #表示是文件的根


#这个是为了确保不同的编辑器下面都一样
[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf  #Unix-style样式的换行符，每个文件都有换行符
insert_final_newline = true
trim_trailing_whitespace = true  #去除每行的最后的空格
